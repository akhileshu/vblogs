import type{ PrismaClient, Prisma } from "@prisma/client";

export async function {{camelCase action}}(prisma: PrismaClient, data: any) {
  try{
    throw new Error("Not implemented");
  }catch(error){
    throw error
  }
  
}
