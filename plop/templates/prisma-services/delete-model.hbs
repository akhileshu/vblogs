import type{ PrismaClient } from "@prisma/client";

export async function delete{{pascalCase model}}(prisma: PrismaClient, id: string) {
  return prisma.{{camelCase model}}.delete({ where: { id } });
}
