import type{ PrismaClient, Prisma } from "@prisma/client";
import { FieldErrors } from "@/server-actions/types/response";
import { FieldsError } from "@/shared/lib/errors/customError";

export async function create{{pascalCase model}}(prisma: PrismaClient, data: Prisma.{{pascalCase model}}CreateInput) {
  try{
    const fieldErrors: FieldErrors<{{pascalCase model}}SchemaType> = {};
    if (Object.keys(fieldErrors).length > 0) {
      throw new FieldsError(fieldErrors);
    }
  return prisma.{{camelCase model}}.create({ data });
  } catch (error) {
    throw error
  }
}
